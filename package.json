{
  "name": "mashlib-dev",
  "private": true,
  "scripts": {
    "start": "lerna bootstrap && scripts/start",
    "add": "scripts/add",
    "delete": "scripts/delete",
    "reset": "scripts/reset",
    "setup": "scripts/reset"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
